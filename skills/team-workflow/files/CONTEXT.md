# ${PROJECT_NAME} — domain glossary

<!-- The project's shared vocabulary. Define the nouns and rules the whole
     team uses so every agent and document means the same thing by each
     term: domain entities (Task, Task states, date rules), the words used
     in UI copy, and any term with a precise boundary (e.g. "Delayed means
     past due AND nothing active — not merely an old due date"). When a
     piece of work sharpens or renames a term, update this file in the same
     commit. Specs and TASKS entries use these exact words. -->

*(no entries yet — add the first terms as the domain takes shape; one
term per heading or bolded line, definition underneath, with the boundary
cases that make it precise)*
